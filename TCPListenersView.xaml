<Window x:Class="ONIT_Kursov_2.TCPListenersView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ONIT_Kursov_2"
        mc:Ignorable="d"
        Title="TCPListenersView" Height="397.859" Width="304.66">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel>
            <Canvas>
                <DataGrid Canvas.Left="20" Canvas.Top="20" AutoGenerateColumns="True" Height="340" ScrollViewer.CanContentScroll="True" ScrollViewer.VerticalScrollBarVisibility="Auto" x:Name="gridTCPListener" HorizontalAlignment="Left" Width="255" RenderTransformOrigin="0.5,0.5" CanUserAddRows="False" IsReadOnly="False">
                    <DataGrid.RowStyle>
                        <Style TargetType="{x:Type DataGridRow}">
                        </Style>
                    </DataGrid.RowStyle>
                </DataGrid>
            </Canvas>
        </DockPanel>
    </Grid>
</Window>
